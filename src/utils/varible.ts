export enum RepositoryType {
  HTTPS = 'https',
  SSH = 'ssh'
}

export enum UnitStatus {
  NORMAL = 'normal',
  LOCKED = 'locked'
}
export enum UnitType {
  TEST = 'test',
  PRODUCTION = 'production'
}
